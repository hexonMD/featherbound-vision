# FeatherBound Vision — BioCLIP 2 species-ID cloud tier
CPU FastAPI service. POST /identify (image) -> top-k {sci, score} over ~11k birds.
Set VISION_API_KEY. Deployed on the BuildIntel Coolify VM as its own app.
